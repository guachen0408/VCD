<Window x:Class="VacuumDryer.UI.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登入" Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Border Background="#1E1E2E" CornerRadius="16" BorderBrush="#45475A" BorderThickness="1">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 標題 + 關閉按鈕 -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="🔐 使用者登入" FontSize="22" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#CDD6F4"/>
                    <TextBlock x:Name="CurrentUserText" Text="目前：未登入" FontSize="11" 
                               HorizontalAlignment="Center" Foreground="#6C7086" Margin="0,6,0,0"/>
                </StackPanel>
                <TextBlock Text="✕" FontSize="16" Foreground="#6C7086" HorizontalAlignment="Right" 
                           VerticalAlignment="Top" Cursor="Hand" MouseDown="CloseX_MouseDown"
                           ToolTip="關閉"/>
            </Grid>
            
            <!-- 帳號 -->
            <Grid Grid.Row="1" Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="65"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="帳號" VerticalAlignment="Center" FontSize="13" Foreground="#A6ADC8"/>
                <TextBox Grid.Column="1" x:Name="UsernameBox" FontSize="14" Padding="10,8"
                         Background="#313244" Foreground="#CDD6F4" 
                         BorderBrush="#45475A" BorderThickness="1" CaretBrush="#89B4FA">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Grid>
            
            <!-- 密碼 -->
            <Grid Grid.Row="2" Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="65"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="密碼" VerticalAlignment="Center" FontSize="13" Foreground="#A6ADC8"/>
                <PasswordBox Grid.Column="1" x:Name="PasswordBox" FontSize="14" Padding="10,8"
                             Background="#313244" Foreground="#CDD6F4" 
                             BorderBrush="#45475A" BorderThickness="1" CaretBrush="#89B4FA"
                             KeyDown="PasswordBox_KeyDown">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>
            </Grid>
            
            <!-- 錯誤訊息 -->
            <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="#F38BA8" FontSize="12"
                       Margin="65,6,0,0" Visibility="Collapsed"/>
            
            <!-- 按鈕 -->
            <Grid Grid.Row="4" Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Content="登入" Grid.Column="0" Height="38" Margin="3"
                        Foreground="#1E1E2E" FontWeight="Bold" FontSize="13"
                        BorderThickness="0" Click="Login_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" CornerRadius="8" Padding="0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#A6E3A1" Offset="0"/>
                                        <GradientStop Color="#94E2D5" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Content="登出" Grid.Column="1" Height="38" Margin="3"
                        Foreground="#1E1E2E" FontWeight="Bold" FontSize="13"
                        BorderThickness="0" Click="Logout_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="#F9E2AF" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Content="關閉" Grid.Column="2" Height="38" Margin="3"
                        Foreground="#CDD6F4" FontWeight="Bold" FontSize="13"
                        BorderThickness="0" Click="Close_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="#45475A" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#585B70"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            
            <!-- 權限說明 -->
            <Border Grid.Row="5" Background="#313244" CornerRadius="10" Padding="12" Margin="0,18,0,0">
                <StackPanel>
                    <TextBlock Text="權限等級" FontWeight="SemiBold" FontSize="11" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                    <TextBlock Text="👁  Viewer  —  僅檢視監控畫面" FontSize="11" Foreground="#6C7086" Margin="0,2"/>
                    <TextBlock Text="🔧 Operator — 啟動/停止流程" FontSize="11" Foreground="#6C7086" Margin="0,2"/>
                    <TextBlock Text="⚙️  Engineer — 修改配方、機台設定" FontSize="11" Foreground="#6C7086" Margin="0,2"/>
                    <TextBlock Text="🔑 Admin  —  管理帳號、完整權限" FontSize="11" Foreground="#6C7086" Margin="0,2"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
