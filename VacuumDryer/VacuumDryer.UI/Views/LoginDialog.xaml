<Window x:Class="VacuumDryer.UI.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登入" Height="320" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 標題 -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="🔓 使用者登入" FontSize="20" FontWeight="Bold" 
                       HorizontalAlignment="Center" Foreground="#333"/>
            <TextBlock x:Name="CurrentUserText" Text="目前：未登入" FontSize="11" 
                       HorizontalAlignment="Center" Foreground="#999" Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- 帳號 -->
        <Grid Grid.Row="1" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="帳號:" VerticalAlignment="Center" FontSize="14"/>
            <TextBox Grid.Column="1" x:Name="UsernameBox" FontSize="14" Padding="8,6"
                     BorderBrush="#CCC" BorderThickness="1"/>
        </Grid>
        
        <!-- 密碼 -->
        <Grid Grid.Row="2" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="密碼:" VerticalAlignment="Center" FontSize="14"/>
            <PasswordBox Grid.Column="1" x:Name="PasswordBox" FontSize="14" Padding="8,6"
                         BorderBrush="#CCC" BorderThickness="1"
                         KeyDown="PasswordBox_KeyDown"/>
        </Grid>
        
        <!-- 錯誤訊息 -->
        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="#F44336" FontSize="12"
                   Margin="70,5,0,0" Visibility="Collapsed"/>
        
        <!-- 按鈕 -->
        <Grid Grid.Row="4" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Content="登入" Grid.Column="0" Height="36" Margin="3"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold" FontSize="14"
                    BorderThickness="0" Click="Login_Click"/>
            <Button Content="登出" Grid.Column="1" Height="36" Margin="3"
                    Background="#FF9800" Foreground="White" FontWeight="Bold" FontSize="14"
                    BorderThickness="0" Click="Logout_Click"/>
            <Button Content="關閉" Grid.Column="2" Height="36" Margin="3"
                    Background="#9E9E9E" Foreground="White" FontWeight="Bold" FontSize="14"
                    BorderThickness="0" Click="Close_Click"/>
        </Grid>
        
        <!-- 權限說明 -->
        <Border Grid.Row="5" Background="#E8E8E8" CornerRadius="5" Padding="10" Margin="0,15,0,0">
            <StackPanel>
                <TextBlock Text="權限等級說明" FontWeight="Bold" FontSize="11" Margin="0,0,0,5"/>
                <TextBlock Text="👁 Viewer — 僅檢視監控畫面" FontSize="11" Foreground="#666"/>
                <TextBlock Text="🔧 Operator — 啟動/停止流程、手動操作" FontSize="11" Foreground="#666"/>
                <TextBlock Text="⚙️ Engineer — 修改配方、機台設定、JOG" FontSize="11" Foreground="#666"/>
                <TextBlock Text="🔑 Admin — 管理帳號、完整權限" FontSize="11" Foreground="#666"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
